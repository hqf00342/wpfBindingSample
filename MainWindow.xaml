<Window x:Class="wpfBindingSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpfBindingSample"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:ViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <local:MyDataGrid Grid.Column="0" DataContext="{Binding}" />

        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />

        <local:MyCanvas Grid.Column="2" Items="{Binding RectInfoCollection}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" />
    </Grid>
</Window>